import { requirePlayer, rpc, escapeHtml } from '../platform/api.mjs';
const $=id=>document.getElementById(id);await requirePlayer();
export function winRate(wins,games){return games?Math.round((Number(wins||0)/Number(games))*100):0}
function bar(label,value,total){const pct=total?Math.round((Number(value||0)/Number(total))*100):0;return `<div class="bar-row"><span>${escapeHtml(label)}</span><span class="bar-track"><span class="bar-fill" style="width:${pct}%"></span></span><strong>${value} · ${pct}%</strong></div>`}
function timeLabel(seconds,increment){const min=seconds>=60?`${Math.round(seconds/60)} د`:`${seconds} ث`;return `${min}${increment?` +${increment}`:''}`}
async function load(){try{const [summaryRows,times,history,achievements]=await Promise.all([rpc('v2_stats_summary'),rpc('v2_stats_time_controls'),rpc('v2_stats_rating_history',{p_limit:120}),rpc('v2_list_achievements')]);const s=Array.isArray(summaryRows)?summaryRows[0]:summaryRows;if(!s)throw new Error('stats unavailable');$('ratingStat').textContent=String(s.rating??1500);$('gamesStat').textContent=String(s.total_games||0);$('winRateStat').textContent=`${winRate(s.wins,s.total_games)}%`;$('puzzleRatingStat').textContent=String(s.puzzle_rating??1200);$('resultStats').innerHTML=bar('فوز',s.wins,s.total_games)+bar('تعادل',s.draws,s.total_games)+bar('خسارة',s.losses,s.total_games);$('colorStats').innerHTML=bar('فوز بالأبيض',s.white_wins,s.white_games)+bar('فوز بالأسود',s.black_wins,s.black_games)+`<div class="platform-row"><span>مباريات الأبيض</span><strong>${s.white_games}</strong></div><div class="platform-row"><span>مباريات الأسود</span><strong>${s.black_games}</strong></div>`;$('timeControlStats').innerHTML=(times||[]).map(t=>`<div class="platform-row"><div><strong>${timeLabel(t.base_seconds,t.increment_seconds)}</strong><div class="platform-muted" style="font-size:11px">${t.games} مباراة</div></div><div>${bar('فوز',t.wins,t.games)}</div></div>`).join('')||'<div class="platform-empty">لا توجد مباريات منتهية في V2 بعد.</div>';$('puzzleStats').innerHTML=`<div class="platform-row"><span>محاولات الألغاز</span><strong>${s.puzzle_attempts}</strong></div><div class="platform-row"><span>ألغاز محلولة</span><strong>${s.puzzle_solves}</strong></div><div class="platform-row"><span>سلسلة الألغاز</span><strong>${s.puzzle_streak}</strong></div><div class="platform-row"><span>سلسلة اليومي</span><strong>${s.daily_puzzle_streak}</strong></div><div class="platform-row"><span>الدروس المكتملة</span><strong>${s.lessons_completed}</strong></div>`;renderHistory(history||[]);$('achievementList').innerHTML=(achievements||[]).map(a=>`<div class="achievement ${a.unlocked?'unlocked':''}"><span class="achievement-icon">${escapeHtml(a.icon)}</span><span><strong>${escapeHtml(a.title)}</strong><div class="platform-muted" style="font-size:11px">${escapeHtml(a.description)}</div></span><span class="platform-badge">${a.unlocked?'مفتوح':'مغلق'}</span></div>`).join('');$('statsStatus').textContent=`${s.achievements_unlocked} إنجاز مفتوح.`}catch(e){console.error(e);$('statsStatus').className='platform-card full platform-error';$('statsStatus').textContent='تعذر تحميل الإحصائيات.'}}
function renderHistory(rows){const host=$('ratingHistory');if(!rows.length){host.innerHTML='<div class="platform-empty">سيظهر تاريخ النقاط بعد المباريات المصنفة.</div>';$('ratingHistoryLabel').textContent='';return}const vals=rows.map(r=>Number(r.rating_after));const min=Math.min(...vals),max=Math.max(...vals),span=Math.max(1,max-min);host.innerHTML=rows.map(r=>{const h=20+((Number(r.rating_after)-min)/span)*150;return `<span class="history-bar" title="${r.rating_after} (${Number(r.delta)>=0?'+':''}${r.delta})" style="height:${h}px"></span>`}).join('');$('ratingHistoryLabel').textContent=`من ${vals[0]} إلى ${vals.at(-1)} · أعلى ${max} · أدنى ${min}`}
await load();
