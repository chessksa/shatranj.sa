name: Test approved home hero redesign

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run approved homepage design tests
        run: |
          python -m pip install pytest
          python -m pytest -q tests/test_home_hero_design.py
