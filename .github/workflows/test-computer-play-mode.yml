name: Test computer play mode
on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'play-v10.html'
      - 'play-v10-match.js'
      - 'play-computer.js'
      - 'vendor/stockfish/**'
      - 'supabase/functions/computer-game/**'
      - 'supabase/migrations/20260906_computer_game_ratings.sql'
      - 'supabase/migrations/20260906_time_controls_5_10_15.sql'
      - 'tests/test_computer_play_mode.py'
      - '.github/workflows/test-computer-play-mode.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run computer play contract
        run: python tests/test_computer_play_mode.py
      - name: Syntax-check computer controller
        run: |
          cp play-computer.js /tmp/play-computer.mjs
          node --check /tmp/play-computer.mjs
# fresh verification trigger for rated computer monotonic clock
