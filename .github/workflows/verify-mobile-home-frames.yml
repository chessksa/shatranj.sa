name: Verify mobile home frames

on:
  push:
    branches:
      - main
    paths:
      - 'home-mobile-admin-colors.css'
      - 'home-header-controls.js'
      - 'index.html'
      - 'tests/test_mobile_home_frames.py'
      - '.github/workflows/verify-mobile-home-frames.yml'
  pull_request:
    paths:
      - 'home-mobile-admin-colors.css'
      - 'home-header-controls.js'
      - 'index.html'
      - 'tests/test_mobile_home_frames.py'
      - '.github/workflows/verify-mobile-home-frames.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify mobile home frame contract
        run: python tests/test_mobile_home_frames.py
      - name: Verify mobile header controls JavaScript
        run: node --check home-header-controls.js
